rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Templates collection - Admin only (temporarily allowing any authenticated user)
    match /templates/{templateId} {
      allow read, write: if request.auth != null;
    }
    
    // Services collection - Admin only (temporarily allowing any authenticated user)
    match /services/{serviceId} {
      allow read, write: if request.auth != null;
    }
    
    // Intakes collection - Public read/write for specific intake, Admin full access
    match /intakes/{intakeId} {
      allow read, write: if true; // Public access for intake forms
      allow read, write: if request.auth != null; // Admin access
    }
    
    // Document Artifacts collection - Admin only (temporarily allowing any authenticated user)
    match /documentArtifacts/{artifactId} {
      allow read, write: if request.auth != null;
    }
    
    // User profiles - User can manage their own profile
    match /users/{userId} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
  }
}